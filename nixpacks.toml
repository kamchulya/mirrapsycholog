[phases.setup]
nixPkgs = ["rustc", "cargo", "gcc", "libpq"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install wheel setuptools",
  "pip install -r requirements.txt"
]

[start]
cmd = "python main.py"
